enum EventListScopeFilter { all, mine, participating, applied, archived }

enum EventListDateSort { newestFirst, oldestFirst }

enum EventListPriceSort { none, ascending, descending }

enum EventVoteViewMode { table, list }
